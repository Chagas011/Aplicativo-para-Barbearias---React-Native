import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    backgroundColor: "#11111A",
    borderRadius: 16,
    padding: 16,
    borderWidth: 1,
    borderColor: "#1F1F2E",
    gap: 10,
  },

  header: {
    flexDirection: "row",
    alignItems: "center",
    gap: 15,
  },

  content: {
    gap: 1,
  },
});
